.env
.venv/
__pycache__/
*.py[cod]
.chroma/
.pytest_cache/
.ruff_cache/
.DS_Store

# Assessment brief, not source.
*.pdf

# Playwright MCP session artifacts
.playwright-mcp/

# Stray screenshots dropped at the repo root by tooling. `screenshots/` itself
# stays tracked -- WALKTHROUGH.md embeds those four, and the brief asks for a
# markdown walkthrough with screenshots.
/*.png
